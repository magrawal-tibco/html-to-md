# Phase 1 — example phase (replace with actual product sitemap URLs)
# Each entry is a product-level sitemapindex URL from the master sitemap.
# All versions under each product are discovered automatically.
name: "Phase 4 - POC 4"
products:
  - https://docs.tibco.com/ftp_portal/coveo/tibco-activematrix-businessworks-plug-in-for-database.xml
